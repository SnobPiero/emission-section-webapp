/**
 * Defines a constant object `AppKeys` that contains key-value pairs for storing language related information.
 *
 * @property {string} storageLanguageKey - The key used for storing the selected language in storage.
 * @property {string} defaultLanguage - The default language value.
 */
export const AppKeys = {
  storageLanguageKey: '@language',
  defaultLanguage: 'en',
};
